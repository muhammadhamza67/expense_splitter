name: expense_splitter_lite
description: A Flutter expense splitter app.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.5
  google_fonts: ^6.3.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  share_plus: ^12.0.0
  uuid: ^4.5.1
  intl: ^0.20.2
  flutter_slidable: ^4.0.3
  flutter_svg: ^2.2.0
  animate_do: ^4.2.0
  dotted_border: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  